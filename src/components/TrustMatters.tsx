import React from 'react';

export function TrustMatters() {
  return (
    <section id="trust" className="centered" data-active="none" style={{ background: 'linear-gradient(180deg,#050505,#06090b)' }}>
      <span className="eyebrow e sr">The Core Premise</span>
      <h2 className="sr sr-delay-1" style={{ maxWidth: '720px' }}>
        Trust Is The New<br /><span className="accent-e">Security Layer</span>
      </h2>
      <p className="body-text centered sr sr-delay-2">
        Intelligent systems need more than protection. True security requires orchestrated trust — from prompt to execution to outcome.
      </p>
    </section>
  );
}
